<h1> All movies </h1>
<ul>
  <% @movies.each do |movie| %>
  <li><%= link_to movie.name, url_for(:movies, :show, id: movie.id) %></li>
  <% end %>
</ul>
<%= link_to("New movie", url_for(:movies, :new)) %>
